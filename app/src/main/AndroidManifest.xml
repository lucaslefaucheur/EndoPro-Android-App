<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="comp354.concordia.endopro">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Hong.Settings" />
        <activity android:name=".Hong.SignUp" />
        <activity android:name=".Hong.test" />
        <activity android:name="comp354.concordia.endopro.Lucas.Dashboard"></activity>
        <activity android:name="comp354.concordia.endopro.Earl.FetchActivity"/>
        <service
            android:name=".Hong.StorageIntent"
            android:exported="false"></service>
    </application>

</manifest>